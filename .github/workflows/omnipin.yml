name: Deploy with Omnipin

on:
  push:
    branches: omnipin

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install bun
        uses: oven-sh/setup-bun@v2

      - name: Install Omnipin
        run: pnpm i -g omnipin@1.6.2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Build website
        run: pnpm i && pnpm run build

      - name: Deploy the site
        run: omnipin deploy src/dist --strict --ens ${{ vars.OMNIPIN_ENS }} --safe ${{ vars.OMNIPIN_SAFE }} --providers Storacha,Lighthouse
        env:
          # Storacha for uploads
          STORACHA_TOKEN: ${{ secrets.OMNIPIN_STORACHA_TOKEN }}
          STORACHA_PROOF: ${{ secrets.OMNIPIN_STORACHA_PROOF }}

          # Filebase for pinning
          # OMNIPIN_FILEBASE_TOKEN: ${{ secrets.OMNIPIN_FILEBASE_TOKEN }}
          # OMNIPIN_FILEBASE_BUCKET_NAME: ${{ secrets.OMNIPIN_FILEBASE_BUCKET_NAME }}

          # Lighthouse for pinning
          OMNIPIN_LIGHTHOUSE_TOKEN: ${{ secrets.OMNIPIN_LIGHTHOUSE_TOKEN }}

          # Wallet that proposes transactions to the Safe
          OMNIPIN_PK: ${{ secrets.OMNIPIN_PK }}
