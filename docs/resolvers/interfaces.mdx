import { interfaceDetails } from "#/data/interfaces";
import { resolver_methods } from "#/data/resolver";

{/* * @type {import('@/lib/mdxPageProps').MdxMetaProps} */}
export const meta = {
    description: 'All interfaces that a resolver may implement.',
    emoji: '👉',
    contributors: [
        'luc.eth',
        'serenae.eth'
    ]
};

# Resolver Interface Standards

This page is a collection of methods that a resolver MAY implement.

{interfaceDetails(resolver_methods)}