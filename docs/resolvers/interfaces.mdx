import { WIP } from "@/components/wip/WIP";
import { resolver_methods } from "#/data/resolver";
import { h2, h3, h4 } from '@/components/mdx/heading/h2';

{/* * @type {import('@/lib/mdxPageProps').MdxMetaProps} */}
export const meta = {
    description: '',
    emoji: '👉',
    contributors: []
};

export const methods = resolver_methods;

# Interface Standards

This page is a collection of methods that a resolver MAY implement

<table>
    <thead>
        <tr>
            <th>Usage</th>
            <th>Interface ID</th>
        </tr>
    </thead>
    <tbody>
        {methods.map((method) => {
            return (
                <tr>
                    <td>
                        <b>{method.usage}</b>
                    </td>
                    <td>{method.name}</td>
                </tr>
            );
        })}
    </tbody>
</table>

## Check Interface Support

```
function supportsInterface(bytes4 interfaceID) external pure returns (bool)
```

{
    methods.map((method) => {
        return (
            <div>
                <h2 id={method.interface}>{method.usage}</h2>
                    <CodeGroup title="Function">
                        <code>{method.name}</code>
                    </CodeGroup>
                    <p>
                        <span>Interface ID is </span>
                        <span>{method.interface}</span>
                    </p>
                    {method.seeMore}
                    {method.output && (
                        <>
                            <h3>Outputs</h3>
                            <p>{method.output}</p>
                        </>
                    )}
            </div>
        );
    })
}