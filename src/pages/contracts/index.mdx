---
description: ENS Smart Contracts Overview
---

import { EmbedLink } from '../../components/EmbedLink'
import { Repository } from '../../components/Repository'

# Smart Contracts

The Ethereum Name Service is built on a set of smart contracts that store and manage information associated with names. ENS v2 introduces hierarchical registries, native L2 support, and a flexible role-based permissions model.

## Architecture

<EmbedLink
  title="Architecture"
  description="Deep dive into the ENS v2 contract architecture, registry hierarchy, and permissions model"
  href="/contracts/architecture"
/>

## Resolution

The resolution process outlines how a name is resolved.
This includes the process of finding a resolver, and then using that resolver to fetch information about a name.

<EmbedLink
  title="Resolution Process"
  description="The resolution process"
  href="/resolution"
/>

## Resolvers

Every name has a resolver, responsible for fetching information about a name, such as address, avatar, and more.
Resolvers allow for programmatic control over the information associated with a name, implemented in solidity.

There are various resolvers to choose from, such as the [Public Resolver](/resolvers/public) or [write your own](/resolvers/writing).

<EmbedLink
  title="Resolvers Quickstart"
  description=""
  href="/resolvers/quickstart"
/>

## Registrars

<div>
  <div className="grid grid-cols-3 gap-4">
    {[
      ['DNS Registrar', '/registry/dns'],
      ['Reverse Registrar', '/registry/reverse'],
    ].map((list) => (
      <EmbedLink
        title={list[0]}
        description=""
        href={list[1]}
        key={list[1]}
        className="p-4"
      />
    ))}
  </div>
</div>

## Source Code

<div>
  <Repository
    src="ensdomains/namechain"
    description="ENS v2 Smart Contracts"
  />
  <Repository
    src="ensdomains/ens-contracts"
    description="ENS v1 Smart Contracts (legacy)"
  />
</div>

---

Looking for v1 documentation? See [Legacy (v1)](/v1).
